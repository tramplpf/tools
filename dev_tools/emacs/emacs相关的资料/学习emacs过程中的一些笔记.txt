学习emacs 过程中的一些笔记：
	Emacs中的模式分为主模式(major mode) 和 次要模式(minor mode) 两种。主模式由文件类型决定，每个buffer打开文件时会根据文件类型决定其主要模式。 
		对应一个buffer 来说，当前的主要模式只能有一种，当指定主要模式时， 原来的主要模式会被替换；而当前的次要模式可以有多种。
		如果输入的模式包含在当前模式中，对应主要模式，会清空所有的次要模式； 对应次要模式，会关闭这个次要模式。 (???? 不懂什么意思)
		模式的变化在状态栏中可以看到