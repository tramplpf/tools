PLSQL 批量生成创建视图的脚本

Tools --》 Export User Objects ， 
会弹出相关的数据，其中包括数据库指定用户下的所有表，序列，视图等。
勾选要导出的对象，
在下面勾选部分，只勾选 "single File",不要勾选其他的内容。然后指定
导出文件的路径和名称。 
这里文件名一般是： create_对象名.sql 
对象的命名规范是：
视图以V_开头， 序列以 _SEQ结尾，表的命名以模块来进行区分。 
